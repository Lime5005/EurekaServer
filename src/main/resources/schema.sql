CREATE TABLE DOG (
                     id INT PRIMARY KEY,
                     name VARCHAR(250) NOT NULL,
                     breed VARCHAR(250) NOT NULL,
                     origin VARCHAR(250) DEFAULT NULL
);